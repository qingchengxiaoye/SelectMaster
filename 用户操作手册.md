# SelectMaster - IP地址与Bat文件映射管理
## 用户操作手册

---

## 目录

1. [软件简介](#软件简介)
2. [功能概述](#功能概述)
3. [界面说明](#界面说明)
4. [快速入门](#快速入门)
5. [详细操作指南](#详细操作指南)
6. [定时执行功能](#定时执行功能)
7. [常见问题](#常见问题)
8. [使用技巧](#使用技巧)

---

## 软件简介

SelectMaster是一款专业的IP地址与Bat文件映射管理工具，用于监控HTTP地址的可用性，并在地址可访问时自动执行对应的Bat批处理文件。该工具支持优先级管理、定时执行和自动切换功能，适用于需要根据网络状态自动执行脚本的应用场景。

**主要特点：**
- 支持多IP地址优先级配置
- HTTP地址实时监控
- Bat文件自动执行
- 定时检查与自动切换
- 完整的日志记录
- 配置导入导出功能

---

## 功能概述

### 核心功能

1. **映射管理**
   - 添加、编辑、删除IP地址与Bat文件的映射关系
   - 设置映射的优先级（数值越小优先级越高）
   - 启用或禁用单个映射

2. **HTTP检查**
   - 实时检测HTTP地址的可用性
   - 自定义超时时间
   - 详细的检查结果反馈

3. **自动执行**
   - 地址可访问时自动执行对应的Bat文件
   - 弹窗显示执行结果
   - 支持按优先级顺序检查

4. **定时功能**
   - 设置定时检查间隔
   - 支持最大执行次数限制
   - 实时倒计时显示
   - 自动切换更高优先级地址

5. **配置管理**
   - 配置文件保存与加载
   - 导出配置到文件
   - 从文件导入配置

---

## 界面说明

### 主界面布局

主界面分为以下几个区域：

```
┌─────────────────────────────────────────────────────┐
│  标题栏 - IP地址与Bat文件映射管理                   │
├─────────────────────────────────────────────────────┤
│                                                     │
│  映射数据网格（DataGridView）                        │
│  - 显示所有映射关系                                  │
│  - 支持直接编辑                                      │
│                                                     │
├─────────────────────────────────────────────────────┤
│  按钮区域                                            │
│  [执行检查] [测试当前地址] [导入配置] [导出配置]       │
│  [添加] [删除] [保存] [选择Bat]                      │
├─────────────────────────────────────────────────────┤
│  定时器设置区域                                      │
│  第一行：下次执行时间 | 总执行次数 | 当前执行信息      │
│  第二行：启用 | 执行间隔 | 最多执行 | 一直执行         │
│          | 启动定时 | 停止定时 | 状态                 │
├─────────────────────────────────────────────────────┤
│  执行日志区域                                        │
│  - 显示详细的执行记录                                │
│  - 自动滚动显示最新内容                              │
├─────────────────────────────────────────────────────┤
│  状态栏 - 显示当前操作状态                           │
└─────────────────────────────────────────────────────┘
```

### 映射数据网格列说明

| 列名 | 说明 | 编辑方式 |
|------|------|---------|
| 启用 | 是否启用此映射 | 点击复选框 |
| 优先级 | 优先级数值（越小越高） | 直接输入数字 |
| HTTP地址 | 要监控的HTTP地址 | 直接输入URL |
| Bat文件路径 | Bat文件的完整路径 | 直接输入或点击"选择Bat" |
| 超时(ms) | 检查超时时间（毫秒） | 直接输入数字 |

### 定时器区域说明

**第一行信息：**
- **下次执行**：显示下次执行的时间倒计时，绿色粗体字
- **总执行次数**：显示已执行的总次数
- **当前执行**：显示当前正在执行的IP地址和优先级

**第二行控制：**
- **启用**：勾选后定时器才会真正运行
- **执行间隔**：设置检查间隔（秒），默认60秒
- **最多执行**：设置最大执行次数，配合"一直执行"选项使用
- **一直执行**：勾选后忽略"最多执行"限制，无限执行
- **启动定时**：绿色按钮，点击后启动定时检查，显示启动时间
- **停止定时**：红色按钮，点击后停止定时检查
- **状态**：显示定时器当前状态（未启动/运行中）

---

## 快速入门

### 第一步：添加映射关系

1. 点击"添加"按钮，在数据网格中新增一行
2. 填写映射信息：
   - 勾选"启用"复选框
   - 设置"优先级"（例如：1, 2, 3...）
   - 输入"HTTP地址"（例如：http://192.168.1.1）
   - 点击"选择Bat"按钮或直接输入Bat文件路径
   - 设置"超时"时间（例如：3000毫秒）
3. 点击"保存"按钮保存配置

### 第二步：测试地址

1. 在数据网格中选择要测试的映射行
2. 点击"测试当前地址"按钮
3. 查看日志区域的检查结果

### 第三步：执行检查

1. 确保已添加并保存映射关系
2. 点击"执行检查"按钮
3. 程序将按优先级顺序检查所有启用的地址
4. 找到可访问的地址后执行对应的Bat文件
5. 查看执行结果和日志

### 第四步：启用定时检查（可选）

1. 勾选"启用"复选框
2. 设置"执行间隔"（例如：60秒）
3. 根据需要设置"最多执行"或勾选"一直执行"
4. 点击"启动定时"按钮（绿色）
5. 观察倒计时和执行状态

---

## 详细操作指南

### 1. 添加映射关系

#### 操作步骤

1. **点击"添加"按钮**
   - 位置：按钮区域
   - 功能：在数据网格底部新增一行空映射

2. **填写映射信息**

   **启用复选框**
   - 勾选：此映射会被检查和执行
   - 不勾选：此映射被忽略

   **优先级**
   - 输入范围：任意正整数
   - 优先级规则：数值越小优先级越高
   - 示例：优先级1 > 优先级2 > 优先级3
   - 注意：相同优先级时按列表顺序检查

   **HTTP地址**
   - 支持格式：http:// 或 https://
   - 示例：
     - http://192.168.1.1
     - https://www.example.com
     - http://localhost:8080
   - 注意：地址必须包含协议前缀

   **Bat文件路径**
   - 方式一：直接输入完整路径
     - 示例：C:\Scripts\switch_network.bat
   - 方式二：点击"选择Bat"按钮
     - 在弹出的文件选择对话框中选择Bat文件
     - 系统会自动填入完整路径
   - 注意：确保路径正确且文件存在

   **超时时间**
   - 单位：毫秒
   - 推荐值：1000-5000毫秒
   - 作用：设置HTTP检查的最大等待时间
   - 注意：网络较慢时可适当增加此值

3. **保存配置**
   - 点击"保存"按钮
   - 配置会自动保存到程序目录
   - 下次启动时会自动加载

#### 示例配置

| 启用 | 优先级 | HTTP地址 | Bat文件路径 | 超时(ms) |
|------|--------|----------|------------|----------|
| ☑ | 1 | http://192.168.1.1 | C:\Scripts\net1.bat | 3000 |
| ☑ | 2 | http://192.168.1.2 | C:\Scripts\net2.bat | 3000 |
| ☑ | 3 | http://192.168.1.3 | C:\Scripts\net3.bat | 3000 |

### 2. 编辑映射关系

#### 直接编辑

1. 在数据网格中点击要编辑的单元格
2. 修改内容
3. 点击"保存"按钮保存更改

#### 复制现有映射

1. 选择要复制的行（点击行头）
2. 复制该行（Ctrl+C）
3. 在新位置粘贴（Ctrl+V）
4. 修改必要信息
5. 点击"保存"

### 3. 删除映射关系

#### 删除单个映射

1. 选择要删除的行
2. 点击"删除"按钮
3. 确认删除
4. 点击"保存"保存更改

#### 批量删除

1. 按住Ctrl键点击多行，或按住Shift键选择连续多行
2. 点击"删除"按钮
3. 确认删除
4. 点击"保存"保存更改

### 4. 导入导出配置

#### 导出配置

1. 配置好所有映射关系
2. 点击"导出配置"按钮
3. 选择保存位置和文件名
4. 点击"保存"

**导出文件用途：**
- 备份当前配置
- 在其他机器上使用相同配置
- 共享配置给其他用户

#### 导入配置

1. 点击"导入配置"按钮
2. 选择之前导出的配置文件
3. 点击"打开"
4. 检查导入的配置是否正确
5. 点击"保存"确认

**注意事项：**
- 导入会覆盖当前配置，建议先备份
- 确保Bat文件路径在当前机器上存在
- 如路径不同，导入后需要手动修改

### 5. 测试地址

#### 测试单个地址

1. 在数据网格中选择要测试的映射行
2. 点击"测试当前地址"按钮
3. 查看日志区域的详细结果

**测试结果说明：**
- ✓ 可访问：地址可以正常访问，显示响应时间
- ✗ 不可访问：地址无法访问，显示错误信息

#### 测试所有地址

点击"执行检查"按钮会测试所有启用的地址。

### 6. 执行检查

#### 手动执行

1. 确保映射关系已配置并保存
2. 点击"执行检查"按钮
3. 程序按优先级顺序检查所有地址
4. 找到可访问地址后执行对应的Bat文件

**执行流程：**
1. 检查优先级最高的地址
2. 如果可访问 → 执行Bat → 结束
3. 如果不可访问 → 检查下一个地址
4. 重复直到找到可访问地址或检查完所有地址

**执行方式：**
- Bat文件在弹窗中执行
- 执行结果实时显示
- 成功/失败状态和退出码都会记录

### 7. 使用日志

#### 查看日志

- 日志实时显示在日志区域
- 自动滚动到最新内容
- 包含时间戳和详细信息

#### 日志内容

**检查日志：**
- 检查时间
- 检查的地址
- 检查结果（可访问/不可访问）
- 响应时间（如果可访问）

**执行日志：**
- 执行的Bat文件
- 执行开始时间
- 执行结果（成功/失败）
- 退出码

**错误日志：**
- 异常信息
- 堆栈跟踪

#### 清空日志

日志会自动保留，无需手动清空。程序关闭后日志会重置。

---

## 定时执行功能

### 功能说明

定时执行功能可以自动按设定间隔检查IP地址并执行Bat文件，无需手动操作。

### 基本设置

#### 启用定时器

1. 勾选"启用"复选框
2. 这个开关控制定时器是否真正运行
3. 即使启动了定时器，如果不勾选"启用"，也不会执行

#### 设置执行间隔

1. 在"执行间隔"文本框中输入秒数
2. 推荐值：30-300秒（5分钟）
3. 最小值：1秒
4. 示例：
   - 60秒 = 1分钟
   - 300秒 = 5分钟
   - 600秒 = 10分钟

#### 设置执行次数限制

**方式一：设置最大执行次数**

1. 不勾选"一直执行"
2. 在"最多执行"文本框中输入次数
3. 示例：输入10表示最多执行10次后停止

**方式二：无限执行**

1. 勾选"一直执行"
2. 系统会忽略"最多执行"的限制
3. 永远执行，直到手动停止

### 启动定时执行

#### 操作步骤

1. 配置好映射关系并保存
2. 勾选"启用"复选框
3. 设置执行间隔和执行次数
4. 点击"启动定时"按钮（绿色）

**启动后的变化：**
- 按钮文字变为启动时间（如：14:30:25）
- "状态"显示"运行中"
- "下次执行"开始倒计时
- 系统开始按间隔自动检查和执行

### 停止定时执行

#### 操作步骤

1. 点击"停止定时"按钮（红色）
2. "状态"显示"未启动"
3. 倒计时停止
4. "下次执行"显示"--"
5. "总执行次数"重置为0
6. 启动按钮文字恢复为"启动定时"

### 监控定时执行

#### 倒计时显示

- "下次执行"实时显示剩余时间
- 格式：`下次执行: XX秒`
- 绿色粗体字显示

#### 执行次数统计

- "总执行次数"显示已执行的总次数
- 格式：`总执行次数: N`

#### 当前执行信息

- "当前执行"显示当前正在执行的IP
- 格式：`当前执行: http://xxx (优先级N)`
- 优先级高的IP优先显示

#### 状态显示

- 未启动：`状态: 未启动`
- 运行中：`状态: 运行中`
- 状态文字实时更新

### 高级功能：自动切换更高优先级

#### 工作原理

系统会持续监控所有优先级的IP地址：
- 如果当前执行的IP仍然可访问，继续使用
- 如果出现更高优先级（更小数值）的IP可访问，自动切换执行该IP对应的Bat

#### 示例场景

假设有以下配置：
- 优先级1：http://192.168.1.1 → net1.bat
- 优先级2：http://192.168.1.2 → net2.bat
- 优先级3：http://192.168.1.3 → net3.bat

**场景1：最高优先级恢复**
1. 当前执行：优先级3（net3.bat）
2. 检测到优先级1和2都不可访问
3. 下次检查：优先级1恢复可访问
4. 自动切换：执行net1.bat
5. 当前执行变为：优先级1

**场景2：优先级不变**
1. 当前执行：优先级1（net1.bat）
2. 检测到优先级1仍然可访问
3. 不执行任何Bat（避免重复执行）
4. 当前执行保持：优先级1

### 定时执行的最佳实践

#### 推荐设置

**生产环境：**
- 执行间隔：300秒（5分钟）
- 执行次数：勾选"一直执行"

**测试环境：**
- 执行间隔：60秒（1分钟）
- 执行次数：设置具体次数（如10次）

#### 注意事项

1. **Bat文件执行时间**
   - 确保Bat文件执行时间小于检查间隔
   - 如果Bat文件执行时间过长，可能影响下次检查

2. **网络稳定性**
   - 如果网络不稳定，适当增加执行间隔
   - 避免过于频繁的检查导致网络压力

3. **资源占用**
   - 长时间运行注意内存使用
   - 定期查看日志文件大小

4. **错误处理**
   - Bat文件应该有完善的错误处理
   - 避免Bat文件卡住或无限循环

---

## 常见问题

### Q1: 点击"执行检查"没有反应？

**可能原因：**
- 没有启用任何映射
- 映射没有保存
- 所有启用的地址都不可访问

**解决方法：**
1. 确保至少有一个映射被勾选"启用"
2. 点击"保存"按钮
3. 检查日志区域的错误信息
4. 先点击"测试当前地址"测试单个地址

### Q2: Bat文件没有执行？

**可能原因：**
- Bat文件路径不正确
- Bat文件不存在
- Bat文件执行错误

**解决方法：**
1. 检查Bat文件路径是否正确
2. 手动双击Bat文件测试是否能正常执行
3. 查看日志区域的错误信息
4. 确保Bat文件路径中没有中文或特殊字符

### Q3: HTTP检查总是失败？

**可能原因：**
- 地址格式不正确
- 网络连接问题
- 目标服务器未响应
- 防火墙阻止

**解决方法：**
1. 检查地址格式（必须包含http://或https://）
2. 在浏览器中测试地址是否可访问
3. 检查网络连接
4. 增加超时时间
5. 检查防火墙设置

### Q4: 定时器没有自动执行？

**可能原因：**
- 没有勾选"启用"复选框
- 没有启动定时器
- 程序被最小化或失去焦点

**解决方法：**
1. 确保"启用"复选框已勾选
2. 确保"启动定时"按钮已点击（显示时间）
3. 查看状态是否显示"运行中"
4. 检查"下次执行"是否有倒计时

### Q5: 如何知道当前执行的是哪个IP？

**解决方法：**
- 查看"当前执行"标签
- 格式：`当前执行: http://xxx (优先级N)`
- 这个信息会实时更新

### Q6: 执行次数如何计算？

**说明：**
- 每次执行任何一个Bat文件，计数加1
- 不论执行成功还是失败，都会计数
- 停止定时器后计数器重置为0

### Q7: 如何停止正在执行的Bat文件？

**解决方法：**
1. 关闭Bat文件的弹窗
2. 如果弹窗无响应，使用任务管理器结束进程
3. 考虑在Bat文件中添加超时机制

### Q8: 导入配置后Bat文件路径不对怎么办？

**解决方法：**
1. 在数据网格中逐行修改Bat文件路径
2. 使用"选择Bat"按钮重新选择文件
3. 点击"保存"保存修改

### Q9: 程序可以同时运行多个实例吗？

**说明：**
- 建议不要同时运行多个实例
- 可能导致配置文件冲突
- 多个实例会同时执行Bat文件

### Q10: 如何备份数据？

**解决方法：**
1. 使用"导出配置"功能
2. 将配置文件保存到安全位置
3. 定期备份重要配置

---

## 使用技巧

### 技巧1：优先级管理

**最佳实践：**
- 使用连续的数字作为优先级：1, 2, 3, 4...
- 留出空间以便插入：1, 5, 10, 15...
- 定期检查优先级设置，确保符合实际需求

**示例场景：**
- 优先级1：主网络（最稳定）
- 优先级2：备用网络（次稳定）
- 优先级3：测试网络（不稳定）
- 优先级4：应急网络（最后选择）

### 技巧2：网络测试

**测试流程：**
1. 先测试单个地址（使用"测试当前地址"）
2. 确认地址可访问后，再添加到映射列表
3. 测试Bat文件是否正常执行
4. 最后配置定时执行

**测试要点：**
- 检查地址的响应时间
- 确认Bat文件执行结果
- 验证执行时间是否合理

### 技巧3：Bat文件优化

**建议：**
- 添加错误处理代码
- 记录执行日志到文件
- 设置合理的超时时间
- 避免长时间运行的命令

**示例Bat文件：**
```batch
@echo off
echo 开始执行网络切换
echo 时间: %date% %time% >> C:\Logs\network.log

REM 执行实际命令
netsh interface ip set address "以太网" static 192.168.1.100 255.255.255.0

REM 检查执行结果
if %errorlevel% equ 0 (
    echo 网络切换成功
    echo 成功: %date% %time% >> C:\Logs\network.log
) else (
    echo 网络切换失败
    echo 失败: %date% %time% >> C:\Logs\network.log
)

echo 执行完成
```

### 技巧4：日志管理

**查看日志：**
- 程序内日志：实时查看
- 外部日志：在Bat文件中写入日志文件

**日志分析：**
- 统计成功/失败次数
- 分析失败原因
- 优化执行间隔

### 技巧5：配置管理

**命名规范：**
- 配置文件使用描述性名称
- 包含日期或版本号
- 示例：`config_20240130.json`、`production_v1.2.json`

**版本控制：**
- 定期导出配置备份
- 保留历史版本
- 重要修改前先备份

### 技巧6：性能优化

**执行间隔设置：**
- 测试阶段：较短间隔（30-60秒）
- 生产环境：适中间隔（300-600秒）
- 稳定阶段：较长间隔（600-1800秒）

**超时设置：**
- 本地地址：1000-2000毫秒
- 内网地址：3000-5000毫秒
- 外网地址：5000-10000毫秒

### 技巧7：错误排查

**问题定位步骤：**
1. 检查日志区域的错误信息
2. 测试单个地址的可访问性
3. 手动执行Bat文件
4. 检查网络连接
5. 查看Bat文件日志

**常见错误：**
- 地址不可访问：检查网络
- Bat执行失败：检查Bat文件
- 超时错误：增加超时时间

### 技巧8：自动化集成

**结合其他工具：**
- 使用Windows任务计划程序自动启动程序
- 配合监控软件使用
- 集成到自动化流程中

**启动参数：**
- 可以为程序创建快捷方式
- 设置开机自启动
- 添加到启动文件夹

---

## 附录

### A. 配置文件格式

配置文件为JSON格式，保存位置：`bin\Debug\net6.0-windows\config.json`

```json
{
  "mappings": [
    {
      "enabled": true,
      "priority": 1,
      "url": "http://192.168.1.1",
      "batFilePath": "C:\\Scripts\\net1.bat",
      "timeout": 3000
    }
  ],
  "timerConfig": {
    "enabled": true,
    "intervalSeconds": 60,
    "maxExecutionCount": -1,
    "infiniteExecution": true,
    "nextExecutionTime": ""
  }
}
```

### B. 快捷键

| 功能 | 快捷键 |
|------|--------|
| 保存配置 | Ctrl+S |
| 执行检查 | F5 |
| 选择行 | 点击行头 |
| 多选行 | Ctrl+点击 或 Shift+点击 |
| 删除行 | Delete |

### C. 文件位置

- 程序目录：`c:\Users\Lenovo\Desktop\SelectMaster\`
- 配置文件：`bin\Debug\net6.0-windows\config.json`
- 执行程序：`bin\Debug\net6.0-windows\SelectMaster.exe`

### D. 技术支持

如遇到问题，请检查：
1. 日志区域的错误信息
2. Bat文件是否正确
3. 网络连接是否正常
4. 配置是否已保存

---

## 更新日志

**v1.0.0** (2024-01-30)
- 初始版本发布
- 支持IP地址与Bat文件映射
- HTTP检查功能
- 自动执行Bat文件
- 定时执行功能
- 配置导入导出
- 优先级管理
- 完整日志记录

---

**文档版本：1.0**
**最后更新：2024-01-30**
**适用软件版本：SelectMaster v1.0**
